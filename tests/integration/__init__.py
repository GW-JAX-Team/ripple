"""Integration tests for ripple waveform models.

This directory contains end-to-end integration tests for ripple's waveform
generation pipeline. These tests verify that waveforms can be generated
successfully and produce valid outputs, without comparing against LALSuite.

These tests run in CI and should complete quickly (< 30 seconds total).
"""
