name: Scheduled dev -> main PR

# Opens a PR merging ripple-dev into main on the last Thursday of each month.
# Cron can't express "last Thursday" directly, so this runs every Thursday
# and the gate step below skips all but the final one in the month.

on:
  schedule:
    - cron: "0 1 * * 4"  # every Thursday, 01:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  open-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Check if today is the last Thursday of the month
        id: gate
        run: |
          today=$(date -u +%-d)
          next_week=$(date -u -d "+7 days" +%-d)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || [ "$next_week" -le "$today" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v7
        if: steps.gate.outputs.run == 'true'
        with:
          fetch-depth: 0
          persist-credentials: false  # only local git reads + gh (GH_TOKEN) are used

      - uses: actions/create-github-app-token@v3
        id: app-token
        if: steps.gate.outputs.run == 'true'
        with:
          client-id: ${{ secrets.AUTOMATION_APP_ID }}  # secret holds the App's Client ID (Iv23...)
          private-key: ${{ secrets.AUTOMATION_APP_PRIVATE_KEY }}
          permission-pull-requests: write  # scope the token: this workflow only opens/updates PRs

      - name: Open or reuse the ripple-dev -> main PR
        id: open_pr
        if: steps.gate.outputs.run == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}  # App token: PRs it opens trigger CI without manual approval
        run: |
          pr_url=$(gh pr list --repo "$GITHUB_REPOSITORY" --base main --head ripple-dev --state open --json url --jq '.[0].url // empty')
          if [ -z "$pr_url" ]; then
            ahead_count=$(git rev-list --count origin/main..origin/ripple-dev)
            if [ "$ahead_count" -eq 0 ]; then
              echo "ripple-dev has no commits ahead of main; nothing to merge."
              exit 0
            fi

            if git diff --quiet origin/main...origin/ripple-dev; then
              echo "ripple-dev has commits ahead of main but no net diff; nothing to merge."
              exit 0
            fi

            pr_url=$(gh pr create \
              --repo "$GITHUB_REPOSITORY" \
              --base main \
              --head ripple-dev \
              --title "Scheduled sync: ripple-dev -> main ($(date -u +'%B %Y'))" \
              --body "Automated monthly pull request merging \`ripple-dev\` into \`main\`.")
          fi

          echo "pr_url=$pr_url" >> "$GITHUB_OUTPUT"

      - name: Request CodeRabbit review
        # CodeRabbit skips auto-review for bot-authored PRs, so ping it explicitly.
        # Kept on GITHUB_TOKEN so the comment is posted as github-actions[bot].
        if: steps.gate.outputs.run == 'true' && steps.open_pr.outputs.pr_url != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr comment "${{ steps.open_pr.outputs.pr_url }}" \
            --repo "$GITHUB_REPOSITORY" \
            --body "@coderabbitai review"
